# 调试示例：表单提交后数据不刷新

## 问题描述
用户提交表单后，列表数据没有更新，必须手动刷新页面才能看到新数据。

## 排查过程
1. 确认提交接口返回 200 ✅
2. 检查提交成功后是否调用了刷新函数 → 发现 `onRefresh` 回调是空函数
3. 定位到父组件没有给 `onRefresh` 传实际函数

## 根因
空回调函数导致刷新逻辑未执行

## 修复
```typescript
// ❌ 错误：空回调
onRefresh={() => {}}

// ✅ 正确：传入实际刷新函数
onRefresh={fetchList}
```
